# Copier vers /srv/lexassist-demo/app/.env puis remplir les secrets.
# Lu par django-environ (config/settings/base.py) depuis BASE_DIR/.env.
DEBUG=False
SECRET_KEY=remplacer-par-un-secret-genere
DATABASE_URL=postgres://lexassist:MOT_DE_PASSE@localhost:5432/lexassist_demo
# db 2 : db 0/1 sont prises par schoolavia (dev/démo) sur le même VPS
REDIS_URL=redis://localhost:6379/2
ALLOWED_HOSTS=cabinet-demo.82.165.110.108.nip.io,82.165.110.108,100.88.169.59,localhost
# L'accès canonique est same-origin (nginx proxifie /api) — le CORS ne sert
# qu'à l'accès de secours par IP brute.
CORS_ALLOWED_ORIGINS=http://82.165.110.108:8090,http://82.165.110.108:8091
# IA documentaire (classification + extraction via Claude)
ANTHROPIC_API_KEY=
