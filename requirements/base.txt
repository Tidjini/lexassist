# ── Core Django ───────────────────────────────────────────────────────────────
Django==6.0.6
djangorestframework==3.17.1
django-cors-headers==4.9.0
django-environ==0.14.0
dj-database-url==2.3.0
whitenoise==6.12.0
gunicorn==26.0.0

# ── Multi-tenant (isolation par schéma PostgreSQL — un cabinet = un schéma) ───
django-tenants==3.10.1

# ── Base de données ───────────────────────────────────────────────────────────
psycopg2-binary==2.9.12

# ── Async / tâches (pipeline IA documentaire en arrière-plan) ─────────────────
celery==5.6.3
redis==8.0.1

# ── WebSocket / notifications temps réel (document traité, alertes) ───────────
channels==4.2.0
channels-redis==4.2.1
daphne==4.1.2

# ── Images (photos de documents) ──────────────────────────────────────────────
Pillow==12.2.0

# ── Export Excel (rapports, export RGPD) ──────────────────────────────────────
openpyxl==3.1.5
xlsxwriter==3.2.0

# ── PDF : écrits/paquets de dépôt + remplissage des formulaires officiels ─────
reportlab==5.0.0
pypdf==6.1.1

# ── IA documentaire (classification + extraction via Claude vision) ───────────
anthropic==0.116.0

# ── Authentification ──────────────────────────────────────────────────────────
djangorestframework-simplejwt==5.3.1

# ── API schema ────────────────────────────────────────────────────────────────
drf-spectacular==0.29.0
django-filter==25.2

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dateutil==2.9.0.post0
django-extensions==4.1
